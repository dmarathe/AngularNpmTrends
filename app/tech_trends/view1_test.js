'use strict';

describe('myApp.trendView module', function() {

  beforeEach(module('myApp.trendView'));

  describe('trendView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl = $controller('TrendCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});